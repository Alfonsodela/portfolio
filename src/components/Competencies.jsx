import styled from '@emotion/styled';

const LayoutCompetencies = styled.div`
  color: var(--color-white);
  background-color: var(--color-pink);
`

const Competencies = () => {
  return (
    <LayoutCompetencies>Competencies</LayoutCompetencies>
  )
}

export default Competencies;